from botlib.core.config_registry import ConfigRegistry
from botlib.core.dropin_registry import DropinRegistry

conf_registry = ConfigRegistry()
registry = DropinRegistry()  # will do the initial load of dropins